export const integrity = {
  levels: {
    quick: {
      title: 'Structure only',
      desc: 'Verifies snapshots, trees and the index. Fast — reads no file data.',
    },
    rotating: {
      title: 'Rotating data part',
      desc: 'Also downloads and verifies one part of the data. Each run continues with the next part, so over time all data is read back.',
    },
    full: {
      title: 'All data',
      desc: 'Downloads and verifies every data pack. Slow, and on cloud storage it can cause significant egress costs.',
    },
    nextPart: (next: number, parts: number) => `Next: part ${next} of ${parts}.`,
  },
  presets: {
    daily: 'Daily at 04:00',
    weekly: 'Weekly — Sunday 04:00',
    monthly: 'Monthly — 1st, 04:00',
    custom: 'Custom…',
  },
  levelLabel: {
    check: 'Check',
    full: 'Full · all data',
    rotating: 'Rotating',
    rotatingPart: (part: number, parts: number) => `Rotating · part ${part}/${parts}`,
    quick: 'Quick · structure',
  },
  verdict: {
    failedNote: (error: string) => ` — the last attempt could not finish: ${error}`,
    damaged: 'Damaged',
    damagedTitle: (at: string) => `restic found integrity errors on ${at}`,
    verified: (relative: string) => `Verified ${relative}`,
    passedTitle: (at: string, level: string) => `No errors found on ${at} (${level})`,
    checkFailed: 'Check failed',
    notVerified: 'Not verified',
    checkFailedTitle: (error: string) => `The integrity check could not finish: ${error}`,
    neverCheckedTitle: 'The repository has never been checked for integrity',
  },
  panel: {
    title: 'Integrity',
    schedule: 'Schedule',
    checkRunning: 'Check running…',
    checkNow: 'Check now',
    damagedAlert: (at: string) =>
      `restic found damaged data in this repository (${at}). Snapshots that reference it may not restore completely. Open the failed check's log below for the affected data and the repair commands restic suggests, then run a new backup and check again.`,
    lastVerdict: 'Last verdict',
    startCheckHint: 'Start a check to verify the repository',
    notScheduled: 'Not scheduled',
    next: (relative: string) => ` · next ${relative}`,
    jobDisabled: ' · job disabled',
    manualOnly: 'Checks only run when started by hand',
    lastAttemptFailed: (summary: string) => `Last attempt could not finish: ${summary}`,
    recentChecks: 'Recent checks',
    noChecks: 'No checks yet.',
    noErrorsFound: 'No errors found',
    integrityErrorsFound: 'Integrity errors found',
    neverChecked: 'Never checked',
  },
  data: {
    label: 'All data read back',
    never: 'Never',
    rotationComplete: (parts: number) => `Rotation complete — the next run starts over at part 1 of ${parts}`,
    rotationProgress: (done: number, parts: number) => `Rotation: ${done} of ${parts} parts read`,
    readBackHint: 'Run a full or rotating check to read the data back',
    partsTitle: (shown: number, parts: number) => `${shown} of ${parts} parts`,
  },
  run: {
    cancelled: 'Check cancelled',
    cancelFailed: 'Could not cancel the check',
    noErrorsFound: 'no errors found',
    integrityErrorsFound: 'integrity errors found',
    scheduled: 'scheduled',
    damaged: 'damaged',
    passed: 'passed',
    duration: 'Duration',
    log: 'Log',
    logTitle: (title: string) => `Check log — ${title}`,
    noOutput: 'No output recorded.',
  },
  checkDialog: {
    title: (job: string) => `Check integrity — ${job}`,
    start: 'Start check',
    started: 'Integrity check started',
    startFailed: 'Could not start the check',
    fullWarning:
      'Every data pack is downloaded from the storage backend. Depending on the repository size this can take hours and cause egress costs.',
    lockAgent: "The check runs on the job's agent and locks the repository while it runs — backups of this job fail during that time.",
    lockLocal: 'The check locks the repository while it runs — backups of this job fail during that time.',
  },
  scheduleDialog: {
    title: (job: string) => `Check schedule — ${job}`,
    partsInvalid: 'Parts must be a whole number between 2 and 100',
    saved: 'Check schedule saved',
    saveFailed: 'Could not save the schedule',
    enable: 'Check this repository on a schedule',
    preset: 'Preset',
    cron: 'Cron',
    cronHelp: 'minute hour day month weekday',
    customSchedule: 'Custom schedule',
    parts: 'Parts',
    partsHelp: (parts: number) =>
      `Each run reads 1/${parts} of the data — after ${parts} successful runs all data has been read back once.`,
    partsRange: 'A whole number between 2 and 100',
    lockNote:
      'Checks lock the repository. Pick a time when this job does not back up; a check that finds the repository busy is skipped until its next turn.',
  },
};

export type IntegrityMessages = typeof integrity;
