export const targets = {
  title: 'Targets',
  subtitle: (n: number) => (n === 1 ? '1 repository' : `${n} repositories`),
  newTarget: 'New target',
  panelTitle: 'Repositories',
  empty: 'No targets yet. Create your first backup repository.',
  row: {
    reachable: 'Reachable',
    offline: (error: string | null) => (error ? `Offline: ${error}` : 'Offline'),
    offlineInline: (error: string | null) => (error ? ` · offline: ${error}` : ' · offline'),
    notChecked: 'Not checked yet',
    checked: (when: string) => `checked ${when}`,
    notCheckedMeta: 'not checked yet',
    checkNow: 'Check now',
    toastReachable: 'Target reachable',
    toastOffline: (error: string | null) => (error ? `Target offline: ${error}` : 'Target offline'),
    noEndpoint: 'This backend has no checkable endpoint',
    checkFailed: 'Check failed',
    edit: 'Edit',
    duplicate: 'Duplicate',
    deleteTitle: 'Delete target',
    deleteConfirm: (name: string) => `"${name}" will be removed. The repository itself stays intact.`,
    deleted: 'Target deleted',
  },
  editor: {
    titleEdit: 'Edit target',
    titleDuplicate: 'Duplicate target',
    titleNew: 'New target',
    create: 'Create',
    copyOf: (name: string) => `Copy of ${name}`,
    saved: 'Target saved',
    saveFailed: 'Save failed',
    name: 'Name',
    namePlaceholder: 'My backup target',
    backend: 'Backend',
    publicKey: 'SFTP public key',
    done: 'Done',
  },
};

export type TargetsMessages = typeof targets;
